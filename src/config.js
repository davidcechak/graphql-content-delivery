const config = {
    // uri and primaryKey should not be commit
    uri: "",
    primaryKey: "",

    database: {
        "id": "Content"
    },

    collection: {
        "id": "Items"
    },
};

module.exports = config;


{
  "name": "graphql-content-delivery",
  "version": "1.0.0",
  "description": "",
  "main": "serve.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/babel src -d out",
    "start": "./node_modules/.bin/babel src -d out & node out/serve.js"
  },
  "dependencies": {
    "documentdb": "^1.13.0",
    "express": "^4.15.4",
    "express-graphql": "^0.6.11",
    "graphql": "^0.11.3",
    "graphql-union-input-type": "^0.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "memoizee": "^0.4.11"
  },
  "babel": {
    "presets": [
      "env"
    ]
  }
}
